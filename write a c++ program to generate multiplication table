#include <iostream>

int main() {
    int number, i;

    std::cout << "Enter the number to generate its multiplication table: ";
    std::cin >> number;

    for (i = 1; i <= 10; ++i) {
        std::cout << number << " * " << i << " = " << number * i << std::endl;
    }

    return 0;
}
